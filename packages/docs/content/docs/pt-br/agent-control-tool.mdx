---
title: Ferramenta de controle para agentes
description: Permita que um agente gerencie sessões, worktrees e tarefas agendadas do MittrCraft pelo chat.
---

# Ferramenta de controle para agentes

Use a ferramenta de agente `mittrcraft` para gerenciar o trabalho no aplicativo diretamente pelo chat. Ela vem ativada por padrão quando o MittrCraft executa seu próprio servidor OpenCode local; não é preciso instalar outra ferramenta nem executar um comando de shell.

## O que você pode pedir

Peça ao agente em linguagem natural. Por exemplo:

- “Crie uma nova sessão do MittrCraft neste projeto, use o modelo `openai/gpt-5.6-sol` e envie este prompt: revise o fluxo de autenticação.”
- “Crie uma nova sessão do MittrCraft para esta tarefa em um worktree separado e peça para ela adicionar testes ao fluxo de login.”
- “Use o MittrCraft para listar minhas 10 sessões mais recentes e inclua o status atual delas.”
- “Crie no MittrCraft uma tarefa agendada chamada Revisão dos dias úteis que envie este prompt às 09:00 de cada dia útil: revise as alterações desde a última execução.”
- “Execute agora a tarefa agendada do MittrCraft chamada Revisão dos dias úteis.”
- “Verifique a sessão do MittrCraft chamada Revisão de autenticação e mostre a resposta mais recente do assistente.”

A ferramenta pode listar projetos e preferências de modelos, criar e continuar sessões, bifurcar uma sessão, criar sessões em worktrees isolados e gerenciar tarefas agendadas. As sessões iniciadas assim aparecem no MittrCraft como qualquer outra, então você pode abri-las e continuar o trabalho por conta própria.

## Lembre-se

- Os prompts de novas sessões retornam imediatamente por padrão. Acompanhe a sessão no MittrCraft ou peça ao agente para verificá-la mais tarde.
- Um worktree separado só é criado quando você pede. As alterações não commitadas do worktree atual não são copiadas.
- A ferramenta não pode excluir sessões ou worktrees, registrar caminhos de projetos, executar comandos de shell arbitrários nem acessar URLs arbitrárias.

## Ativar ou desativar a ferramenta

Abra **Configurações → Geral → Ferramentas do MittrCraft** e altere **Ferramenta de controle para agentes**. A configuração entra em vigor quando o servidor OpenCode gerenciado reinicia, o que o MittrCraft oferece como **Apply & Restart**.

A ferramenta não está disponível quando o MittrCraft se conecta a um servidor OpenCode externo por `OPENCODE_HOST` ou skip-start, nem na extensão do VS Code. As instalações desktop e web que usam o servidor OpenCode gerenciado pelo MittrCraft têm suporte automático.

## Relacionado

- [Tarefas agendadas](/pt-br/scheduled-tasks/)
- [Sessões de worktree](/pt-br/worktrees/)
- [Objetivos de sessão](/pt-br/session-goals/)
- [Painel do navegador](/pt-br/desktop-browser/) — a ferramenta MittrCraft Web, para ver uma página e conduzi-la
