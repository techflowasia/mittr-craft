---
title: Підключення до OpenCode
description: Виправте проблему, коли Mittr Craft не підключається до свого сервера OpenCode.
---

# Підключення до OpenCode

Якщо Mittr Craft завантажується, але ніяк не виходить за межі стану «OpenCode перезапускається», або чат не відповідає, сервер, із яким він спілкується, недоступний. Пройдіться по цьому.

## Він застряг на «OpenCode перезапускається»

- дайте йому хвилину одразу після запуску — цей стан нормальний, поки сервер стартує
- перевірте, чи сервер живий, командою `openchamber status`
- перезапустіть його командою `openchamber restart`
- перегляньте деталі запуску командою `openchamber logs`

## Ви підключаєтеся до власного сервера

Якщо ви налаштували Mittr Craft на використання наявного сервера, ще раз перевірте налаштування з [OpenCode Server](/uk/opencode-server/):

- `OPENCODE_HOST` має містити порт, без шляху — наприклад, `http://localhost:4096`
- задайте `OPENCODE_SKIP_START=true`, щоб Mittr Craft не запускав ще й свій
- якщо адреса недійсна, Mittr Craft ігнорує її й натомість запускає власний сервер — шукайте попередження `[config]` у журналах

## Усе ще не працює

- переконайтеся, що версія Node.js — `20` або новіша
- перевстановіть найновіший CLI
- відкрийте `http://localhost:3000` напряму, перш ніж тестувати будь-який тунель чи віддалене посилання

## Пов'язане

- [OpenCode Server](/uk/opencode-server/) — як Mittr Craft знаходить сервер і керує ним
- [Усунення несправностей](/uk/troubleshooting/) — інші поширені проблеми
