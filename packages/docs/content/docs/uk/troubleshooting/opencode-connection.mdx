---
title: Підключення до OpenCode
description: Виправте проблему, коли MittrCraft не підключається до свого сервера OpenCode.
---

# Підключення до OpenCode

Якщо MittrCraft завантажується, але ніяк не виходить за межі стану «OpenCode перезапускається», або чат не відповідає, сервер, із яким він спілкується, недоступний. Пройдіться по цьому.

## Він застряг на «OpenCode перезапускається»

- дайте йому хвилину одразу після запуску — цей стан нормальний, поки сервер стартує
- перевірте, чи сервер живий, командою `mittrcraft status`
- перезапустіть його командою `mittrcraft restart`
- перегляньте деталі запуску командою `mittrcraft logs`

## Ви підключаєтеся до власного сервера

Якщо ви налаштували MittrCraft на використання наявного сервера, ще раз перевірте налаштування з [OpenCode Server](/uk/opencode-server/):

- `OPENCODE_HOST` має містити порт, без шляху — наприклад, `http://localhost:4096`
- задайте `OPENCODE_SKIP_START=true`, щоб MittrCraft не запускав ще й свій
- якщо адреса недійсна, MittrCraft ігнорує її й натомість запускає власний сервер — шукайте попередження `[config]` у журналах

## Усе ще не працює

- переконайтеся, що версія Node.js — `20` або новіша
- перевстановіть найновіший CLI
- відкрийте `http://localhost:3000` напряму, перш ніж тестувати будь-який тунель чи віддалене посилання

## Пов'язане

- [OpenCode Server](/uk/opencode-server/) — як MittrCraft знаходить сервер і керує ним
- [Усунення несправностей](/uk/troubleshooting/) — інші поширені проблеми
